#include <stdio.h>

int main() {
float notas = 0;
int notasqnt = 0;
    while(notas >= 0 && notas <=10){
        printf("Digite as notas: ");
        scanf("%f", &notas );
    if (notas >= 0 && notas <=10 ){
    }
    }
printf("vc digitou uma nota invalida");
}
